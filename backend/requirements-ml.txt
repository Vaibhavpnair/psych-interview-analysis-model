# AI / ML Dependencies (install separately)
# ─────────────────────────────────────────────────────
# Install with: pip install -r requirements-ml.txt
#
# These are OPTIONAL for running the web server.
# The AI modules use lazy loading and gracefully skip
# if these packages are not installed.

# Vision Module
mediapipe>=0.10.30
opencv-python>=4.10.0
# deepface>=0.0.93  # Uncomment when needed (pulls heavy deps)

# Speech Module
openai-whisper>=20240930
librosa>=0.10.2
soundfile>=0.12.1
# parselmouth>=0.4.4  # Broken on Python 3.13 — use praat-parselmouth instead

# NLP Module
spacy>=3.7.0
transformers>=4.40.0
torch>=2.2.0
vaderSentiment>=3.3.2

# Data
pandas>=2.2.0
scipy>=1.12.0
